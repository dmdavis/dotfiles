#!/usr/bin/env bash

# pip aliases
alias pi='pip install'
alias pir='pip install -r'
alias piu='pip install -U'
alias pie='pip install -e .'
alias pid='pip install  --process-dependency-links'
alias pide='pip install  --process-dependency-links -e .'
